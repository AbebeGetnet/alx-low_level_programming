#include <stdio.h>

/*
 * main print number digits
 *
 * return : always (success)
 */
int main(void)
{
	int n;

	for (n = '0'; n <= 0; n++)
	{
		putchar(n);

		if (c != '9')
		{
			putchar(',');
			putchar(' ');
		}
	}

	putchar('\n');

	return (0);
}
